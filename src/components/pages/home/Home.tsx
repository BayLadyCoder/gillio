import React from "react";
import PropTypes from "prop-types";
import Hero from "../../shared/Hero";

const Home = () => {
  return (
    <div>
      <Hero />
    </div>
  );
};

Home.propTypes = {};

export default Home;
