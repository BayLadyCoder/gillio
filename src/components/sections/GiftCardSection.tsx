import React from "react";
import PropTypes from "prop-types";

const GiftCardSection = () => {
  return <div>GiftCardSection</div>;
};

GiftCardSection.propTypes = {};

export default GiftCardSection;
